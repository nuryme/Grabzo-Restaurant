# Copy to .env.local and fill in. Never commit .env.local.

# Leave empty for local dev — Vite proxies /api and /socket.io to localhost:4000.
# In production (Vercel), set to your Render backend URL, e.g. https://grabzo-server.onrender.com
VITE_API_URL=
